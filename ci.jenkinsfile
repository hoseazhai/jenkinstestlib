pipeline{
    agent { node { label "master"}}
    
    stages{
        stage("build"){
            steps{
                script{
                    mvnhome = tool "maven"
                    sh "${mvnhome}/bin/mvn -v"
                    }
               }
             }
           }
  }
